# 001 — Explorer fold-in + direction correction

## Direction correction (user)
Original explorers partly answered OpenProvider→Grok credentials. User clarified:
**Grok Build should consume OpenProvider models.**

## Peirce (OpenProvider audit)
Still useful for opposite-path context and transport facts; not the primary verdict.

## Primary proof for corrected question
- Grok user-guide 11 custom models + backends
- OpenProvider server routes + live /v1/models on :10100
